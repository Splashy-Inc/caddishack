extends Node

signal bead_color_highlight_toggle_requested(bead_info: BeadInfo, is_highlighted: bool)
signal bead_charm_highlight_toggle_requested(bead_info: BeadInfo, is_highlighted: bool)

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
